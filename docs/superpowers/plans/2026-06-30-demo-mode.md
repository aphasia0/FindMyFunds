# Demo Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere una modalità demo che permette agli utenti non autenticati di esplorare l'intera app con dati statici realistici, accessibile da `/demo` e da un bottone nella pagina di login.

**Architecture:** `DemoModeService` è l'unica fonte di verità (`isDemo: boolean`, persistito in `sessionStorage`). L'`authGuard` bypassa il controllo Supabase quando `isDemo === true`. Tutti i servizi esistenti controllano `isDemo` prima di chiamare Supabase e restituiscono dati statici da `demo-data.ts`.

**Tech Stack:** Angular 17 standalone components, Jasmine/Karma, TypeScript strict.

## Global Constraints

- TypeScript strict — nessun `any` non giustificato, tutti i parametri tipizzati
- Pattern esistente per i test: `TestBed.configureTestingModule` + Jasmine, vedi `dashboard.service.spec.ts`
- Comandi di test: `npx ng test --watch=false --browsers=ChromeHeadless`
- Compilazione: `npx tsc --noEmit` deve passare senza errori dopo ogni task
- Nessun modifica a `supabase.service.ts` o `auth.service.ts`
- Tutti i componenti sono standalone — aggiungere import nei metadata dove necessario

---

## File Map

| File | Azione | Responsabilità |
|---|---|---|
| `src/app/core/demo-mode.service.ts` | Crea | Stato isDemo, enter(), exit() |
| `src/app/core/demo-mode.service.spec.ts` | Crea | Test del servizio |
| `src/app/core/demo-data.ts` | Crea | Dati statici: account, snapshot, income |
| `src/app/features/demo/demo-entry.component.ts` | Crea | Componente route /demo |
| `src/app/core/auth.guard.ts` | Modifica | Bypass guard in demo |
| `src/app/app.routes.ts` | Modifica | Route /demo senza guard |
| `src/app/features/dashboard/dashboard.service.ts` | Modifica | loadAllData() restituisce demo data |
| `src/app/features/accounts/accounts.service.ts` | Modifica | Metodi read restituiscono demo data, write sono no-op |
| `src/app/features/accounts/accounts.component.ts` | Modifica | Nasconde pulsanti azione in demo |
| `src/app/features/monthly-entry/monthly-entry.service.ts` | Modifica | Metodi read restituiscono demo data, write sono no-op |
| `src/app/features/monthly-entry/monthly-entry.component.ts` | Modifica | Disabilita salvataggio in demo, bypassa profileService |
| `src/app/shared/layout/shell/shell.component.ts` | Modifica | Skip onboarding check in demo |
| `src/app/shared/layout/nav/nav.component.ts` | Modifica | Badge DEMO + link Esci dalla demo |
| `src/app/features/auth/login/login.component.ts` | Modifica | Bottone "Prova la demo →" |
| `src/app/features/settings/settings.component.ts` | Modifica | Banner demo al posto del form |

---

## Task 1: DemoModeService e demo-data.ts

**Files:**
- Crea: `src/app/core/demo-mode.service.ts`
- Crea: `src/app/core/demo-mode.service.spec.ts`
- Crea: `src/app/core/demo-data.ts`

**Interfaces:**
- Produce: `DemoModeService.isDemo: boolean`, `DemoModeService.enter(): void`, `DemoModeService.exit(): void`
- Produce: `DEMO_ACCOUNTS: Account[]`, `DEMO_SNAPSHOTS: MonthlySnapshot[]`, `DEMO_INCOME: IncomeTransaction[]`

- [ ] **Step 1: Scrivi il test fallente per DemoModeService**

`src/app/core/demo-mode.service.spec.ts`:
```ts
import { TestBed } from '@angular/core/testing';
import { Router } from '@angular/router';
import { DemoModeService } from './demo-mode.service';

describe('DemoModeService', () => {
  let service: DemoModeService;
  let routerSpy: jasmine.SpyObj<Router>;

  beforeEach(() => {
    sessionStorage.clear();
    routerSpy = jasmine.createSpyObj('Router', ['navigate']);
    TestBed.configureTestingModule({
      providers: [
        DemoModeService,
        { provide: Router, useValue: routerSpy },
      ],
    });
    service = TestBed.inject(DemoModeService);
  });

  it('starts with isDemo false when sessionStorage is empty', () => {
    expect(service.isDemo).toBeFalse();
  });

  it('restores isDemo true from sessionStorage on construction', () => {
    sessionStorage.setItem('fmf_demo', 'true');
    TestBed.resetTestingModule();
    TestBed.configureTestingModule({
      providers: [DemoModeService, { provide: Router, useValue: routerSpy }],
    });
    const s2 = TestBed.inject(DemoModeService);
    expect(s2.isDemo).toBeTrue();
  });

  it('enter() sets isDemo true, persists to sessionStorage, navigates to /dashboard', () => {
    service.enter();
    expect(service.isDemo).toBeTrue();
    expect(sessionStorage.getItem('fmf_demo')).toBe('true');
    expect(routerSpy.navigate).toHaveBeenCalledWith(['/dashboard']);
  });

  it('exit() clears isDemo, removes from sessionStorage, navigates to /auth/login', () => {
    service.enter();
    routerSpy.navigate.calls.reset();
    service.exit();
    expect(service.isDemo).toBeFalse();
    expect(sessionStorage.getItem('fmf_demo')).toBeNull();
    expect(routerSpy.navigate).toHaveBeenCalledWith(['/auth/login']);
  });
});
```

- [ ] **Step 2: Verifica che il test fallisca**

```
npx ng test --watch=false --browsers=ChromeHeadless --include="**/demo-mode.service.spec.ts"
```
Atteso: FAILED — `DemoModeService` non esiste.

- [ ] **Step 3: Crea DemoModeService**

`src/app/core/demo-mode.service.ts`:
```ts
import { Injectable, inject } from '@angular/core';
import { Router } from '@angular/router';

@Injectable({ providedIn: 'root' })
export class DemoModeService {
  private router = inject(Router);

  isDemo = sessionStorage.getItem('fmf_demo') === 'true';

  enter(): void {
    this.isDemo = true;
    sessionStorage.setItem('fmf_demo', 'true');
    this.router.navigate(['/dashboard']);
  }

  exit(): void {
    this.isDemo = false;
    sessionStorage.removeItem('fmf_demo');
    this.router.navigate(['/auth/login']);
  }
}
```

- [ ] **Step 4: Verifica che i test passino**

```
npx ng test --watch=false --browsers=ChromeHeadless --include="**/demo-mode.service.spec.ts"
```
Atteso: 4 tests PASSED.

- [ ] **Step 5: Crea demo-data.ts**

`src/app/core/demo-data.ts`:
```ts
import { Account, MonthlySnapshot, IncomeTransaction } from './models';

export const DEMO_ACCOUNTS: Account[] = [
  { id: 'demo-acc-1', user_id: 'demo', name: 'Conto BancaIntesa', type: 'cash',  asset_description: null, is_active: true, created_at: '2025-01-01T00:00:00Z' },
  { id: 'demo-acc-2', user_id: 'demo', name: 'BTP 2030',          type: 'bond',  asset_description: null, is_active: true, created_at: '2025-01-01T00:00:00Z' },
  { id: 'demo-acc-3', user_id: 'demo', name: 'ETF S&P500',        type: 'etf',   asset_description: null, is_active: true, created_at: '2025-01-01T00:00:00Z' },
  { id: 'demo-acc-4', user_id: 'demo', name: 'Azioni ENI',        type: 'stock', asset_description: null, is_active: true, created_at: '2025-01-01T00:00:00Z' },
  { id: 'demo-acc-5', user_id: 'demo', name: 'Oro fisico',        type: 'asset', asset_description: null, is_active: true, created_at: '2025-01-01T00:00:00Z' },
];

// [year, month, [cash, bond, etf, stock, asset]]
const RAW_SNAPSHOTS: Array<[number, number, [number, number, number, number, number]]> = [
  [2025,  6, [8200, 15000, 24500, 5200, 12000]], // mese contesto per delta
  [2025,  7, [8400, 15050, 25200, 5100, 12150]],
  [2025,  8, [8200, 15100, 25900, 5400, 12300]],
  [2025,  9, [8700, 15150, 26600, 5200, 12450]],
  [2025, 10, [8300, 15200, 25800, 4700, 12600]],
  [2025, 11, [8900, 15250, 27100, 5000, 12750]],
  [2025, 12, [9400, 15300, 28200, 5300, 12900]],
  [2026,  1, [8700, 15350, 28600, 5500, 13050]],
  [2026,  2, [9100, 15400, 29400, 5100, 13200]],
  [2026,  3, [9500, 15450, 30500, 5600, 13350]],
  [2026,  4, [9000, 15500, 29200, 4900, 13500]],
  [2026,  5, [9600, 15550, 30800, 5400, 13650]],
  [2026,  6, [10000, 15600, 31500, 5700, 13800]],
];

const ACC_IDS = DEMO_ACCOUNTS.map(a => a.id);

export const DEMO_SNAPSHOTS: MonthlySnapshot[] = RAW_SNAPSHOTS.flatMap(([year, month, values]) =>
  ACC_IDS.map((account_id, i) => ({
    id: `demo-snap-${year}-${month}-${account_id}`,
    user_id: 'demo',
    account_id,
    year,
    month,
    snapshot_date: `${year}-${String(month).padStart(2, '0')}-27`,
    value: values[i],
    created_at: `${year}-${String(month).padStart(2, '0')}-27T00:00:00Z`,
  }))
);

// Income dal lug 2025 a giu 2026 (12 mesi visualizzati)
const INCOME_MONTHS: Array<[number, number]> = [
  [2025, 7], [2025, 8], [2025, 9], [2025, 10], [2025, 11], [2025, 12],
  [2026, 1], [2026, 2], [2026, 3], [2026, 4],  [2026, 5],  [2026, 6],
];

export const DEMO_INCOME: IncomeTransaction[] = INCOME_MONTHS.map(([year, month]) => ({
  id: `demo-inc-${year}-${month}`,
  user_id: 'demo',
  year,
  month,
  amount: 3500,
  description: 'Stipendio',
  created_at: `${year}-${String(month).padStart(2, '0')}-27T00:00:00Z`,
}));
```

- [ ] **Step 6: Verifica compilazione**

```
npx tsc --noEmit
```
Atteso: nessun errore.

- [ ] **Step 7: Commit**

```bash
git add src/app/core/demo-mode.service.ts src/app/core/demo-mode.service.spec.ts src/app/core/demo-data.ts
git commit -m "feat: add DemoModeService and static demo data"
```

---

## Task 2: Auth guard, DemoEntryComponent e route /demo

**Files:**
- Modifica: `src/app/core/auth.guard.ts`
- Crea: `src/app/features/demo/demo-entry.component.ts`
- Modifica: `src/app/app.routes.ts`

**Interfaces:**
- Consuma: `DemoModeService.isDemo`, `DemoModeService.enter()`
- Produce: route pubblica `/demo` che entra in demo e redirige a `/dashboard`

- [ ] **Step 1: Aggiorna auth.guard.ts**

`src/app/core/auth.guard.ts`:
```ts
import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';
import { filter, map, take } from 'rxjs/operators';
import { Session } from '@supabase/supabase-js';
import { AuthService } from './auth.service';
import { DemoModeService } from './demo-mode.service';

export const authGuard: CanActivateFn = () => {
  const demoMode = inject(DemoModeService);
  if (demoMode.isDemo) return true;

  const auth = inject(AuthService);
  const router = inject(Router);
  return auth.session$.pipe(
    filter((s): s is Session | null => s !== undefined),
    take(1),
    map(session => (session ? true : router.createUrlTree(['/auth/login'])))
  );
};
```

- [ ] **Step 2: Crea DemoEntryComponent**

`src/app/features/demo/demo-entry.component.ts`:
```ts
import { Component, OnInit, inject } from '@angular/core';
import { DemoModeService } from '../../core/demo-mode.service';

@Component({ selector: 'app-demo-entry', standalone: true, template: '' })
export class DemoEntryComponent implements OnInit {
  private demoMode = inject(DemoModeService);

  ngOnInit() {
    this.demoMode.enter();
  }
}
```

- [ ] **Step 3: Aggiungi route /demo in app.routes.ts**

In `src/app/app.routes.ts`, aggiungi prima del catch-all `**`:
```ts
{
  path: 'demo',
  loadComponent: () =>
    import('./features/demo/demo-entry.component').then(m => m.DemoEntryComponent),
},
```

Il file completo diventa:
```ts
import { Routes } from '@angular/router';
import { authGuard } from './core/auth.guard';

export const routes: Routes = [
  { path: '', redirectTo: 'dashboard', pathMatch: 'full' },
  {
    path: 'auth',
    loadChildren: () =>
      import('./features/auth/auth.routes').then(m => m.AUTH_ROUTES),
  },
  {
    path: 'onboarding',
    canActivate: [authGuard],
    loadComponent: () =>
      import('./features/auth/onboarding/onboarding.component').then(
        m => m.OnboardingComponent
      ),
  },
  {
    path: '',
    canActivate: [authGuard],
    loadComponent: () =>
      import('./shared/layout/shell/shell.component').then(m => m.ShellComponent),
    children: [
      {
        path: 'dashboard',
        loadComponent: () =>
          import('./features/dashboard/dashboard.component').then(m => m.DashboardComponent),
      },
      {
        path: 'entry',
        loadComponent: () =>
          import('./features/monthly-entry/monthly-entry.component').then(m => m.MonthlyEntryComponent),
      },
      {
        path: 'accounts',
        loadComponent: () =>
          import('./features/accounts/accounts.component').then(m => m.AccountsComponent),
      },
      {
        path: 'settings',
        loadComponent: () =>
          import('./features/settings/settings.component').then(m => m.SettingsComponent),
      },
    ],
  },
  {
    path: 'demo',
    loadComponent: () =>
      import('./features/demo/demo-entry.component').then(m => m.DemoEntryComponent),
  },
  { path: '**', redirectTo: 'dashboard' },
];
```

- [ ] **Step 4: Verifica compilazione**

```
npx tsc --noEmit
```
Atteso: nessun errore.

- [ ] **Step 5: Commit**

```bash
git add src/app/core/auth.guard.ts src/app/features/demo/demo-entry.component.ts src/app/app.routes.ts
git commit -m "feat: add /demo route and bypass authGuard in demo mode"
```

---

## Task 3: DashboardService — dati demo

**Files:**
- Modifica: `src/app/features/dashboard/dashboard.service.ts`

**Interfaces:**
- Consuma: `DemoModeService.isDemo`, `DEMO_SNAPSHOTS`, `DEMO_INCOME`
- Produce: `loadAllData()` restituisce dati statici in demo mode

- [ ] **Step 1: Aggiorna dashboard.service.ts**

Aggiungi i due import in cima:
```ts
import { DemoModeService } from '../../core/demo-mode.service';
import { DEMO_SNAPSHOTS, DEMO_INCOME } from '../../core/demo-data';
```

Aggiungi l'inject del servizio nella classe:
```ts
private demoMode = inject(DemoModeService);
```

Aggiorna `loadAllData()`:
```ts
async loadAllData(): Promise<{ snapshots: any[]; income: IncomeTransaction[] }> {
  if (this.demoMode.isDemo) {
    return { snapshots: DEMO_SNAPSHOTS, income: DEMO_INCOME };
  }

  const [snapshotsRes, incomeRes] = await Promise.all([
    this.supabase
      .from('monthly_snapshots')
      .select('*, accounts(type)')
      .eq('user_id', this.auth.userId),
    this.supabase
      .from('income_transactions')
      .select('*')
      .eq('user_id', this.auth.userId),
  ]);
  if (snapshotsRes.error) throw snapshotsRes.error;
  if (incomeRes.error) throw incomeRes.error;
  return {
    snapshots: snapshotsRes.data ?? [],
    income: (incomeRes.data ?? []) as IncomeTransaction[],
  };
}
```

Nota: `DEMO_SNAPSHOTS` non include il campo `accounts: { type }` che il service usa per `buildComposition()`. Aggiungi il campo ai demo snapshots nel calcolo. Il modo più semplice è fare il join direttamente in `loadAllData()` quando in demo mode:

```ts
if (this.demoMode.isDemo) {
  const accountTypeMap = new Map(DEMO_ACCOUNTS.map(a => [a.id, a.type]));
  const snapshots = DEMO_SNAPSHOTS.map(s => ({
    ...s,
    accounts: { type: accountTypeMap.get(s.account_id) ?? 'asset' },
  }));
  return { snapshots, income: DEMO_INCOME };
}
```

Aggiungi anche l'import di `DEMO_ACCOUNTS`:
```ts
import { DEMO_SNAPSHOTS, DEMO_INCOME, DEMO_ACCOUNTS } from '../../core/demo-data';
```

Il metodo `loadAllData()` completo:
```ts
async loadAllData(): Promise<{ snapshots: any[]; income: IncomeTransaction[] }> {
  if (this.demoMode.isDemo) {
    const accountTypeMap = new Map(DEMO_ACCOUNTS.map(a => [a.id, a.type]));
    const snapshots = DEMO_SNAPSHOTS.map(s => ({
      ...s,
      accounts: { type: accountTypeMap.get(s.account_id) ?? 'asset' },
    }));
    return { snapshots, income: DEMO_INCOME };
  }

  const [snapshotsRes, incomeRes] = await Promise.all([
    this.supabase
      .from('monthly_snapshots')
      .select('*, accounts(type)')
      .eq('user_id', this.auth.userId),
    this.supabase
      .from('income_transactions')
      .select('*')
      .eq('user_id', this.auth.userId),
  ]);
  if (snapshotsRes.error) throw snapshotsRes.error;
  if (incomeRes.error) throw incomeRes.error;
  return {
    snapshots: snapshotsRes.data ?? [],
    income: (incomeRes.data ?? []) as IncomeTransaction[],
  };
}
```

- [ ] **Step 2: Verifica compilazione + test esistenti**

```
npx tsc --noEmit && npx ng test --watch=false --browsers=ChromeHeadless --include="**/dashboard.service.spec.ts"
```
Atteso: compilazione OK, tutti i test esistenti PASSED.

- [ ] **Step 3: Commit**

```bash
git add src/app/features/dashboard/dashboard.service.ts
git commit -m "feat: dashboard service returns demo data in demo mode"
```

---

## Task 4: AccountsService + AccountsComponent

**Files:**
- Modifica: `src/app/features/accounts/accounts.service.ts`
- Modifica: `src/app/features/accounts/accounts.component.ts`

**Interfaces:**
- Consuma: `DemoModeService.isDemo`, `DEMO_ACCOUNTS`, `DEMO_SNAPSHOTS`
- Produce: read methods restituiscono demo data; write methods sono no-op in demo

- [ ] **Step 1: Aggiorna accounts.service.ts**

Aggiungi import in cima:
```ts
import { DemoModeService } from '../../core/demo-mode.service';
import { DEMO_ACCOUNTS, DEMO_SNAPSHOTS } from '../../core/demo-data';
```

Aggiungi inject nella classe:
```ts
private demoMode = inject(DemoModeService);
```

Sostituisci i metodi con la versione demo-aware:
```ts
async getActiveAccounts(): Promise<Account[]> {
  if (this.demoMode.isDemo) return DEMO_ACCOUNTS.filter(a => a.is_active);

  const { data, error } = await this.supabase
    .from('accounts')
    .select('*')
    .eq('user_id', this.auth.userId)
    .eq('is_active', true)
    .order('created_at');
  if (error) throw error;
  return data as Account[];
}

async getAllAccounts(): Promise<Account[]> {
  if (this.demoMode.isDemo) return [...DEMO_ACCOUNTS];

  const { data, error } = await this.supabase
    .from('accounts')
    .select('*')
    .eq('user_id', this.auth.userId)
    .order('is_active', { ascending: false })
    .order('created_at');
  if (error) throw error;
  return data as Account[];
}

async getLatestBalances(): Promise<Map<string, number>> {
  if (this.demoMode.isDemo) {
    const map = new Map<string, number>();
    for (const snap of DEMO_SNAPSHOTS) {
      if (!map.has(snap.account_id)) map.set(snap.account_id, snap.value);
    }
    return map;
  }

  const { data, error } = await this.supabase
    .from('monthly_snapshots')
    .select('account_id, value, year, month')
    .eq('user_id', this.auth.userId)
    .order('year', { ascending: false })
    .order('month', { ascending: false });
  if (error) throw error;
  const map = new Map<string, number>();
  for (const row of (data ?? [])) {
    if (!map.has(row.account_id)) map.set(row.account_id, Number(row.value));
  }
  return map;
}

async createAccount(dto: AccountFormData): Promise<Account> {
  if (this.demoMode.isDemo) return DEMO_ACCOUNTS[0]; // no-op

  const { data, error } = await this.supabase
    .from('accounts')
    .insert({ ...dto, user_id: this.auth.userId })
    .select()
    .single();
  if (error) throw error;
  return data as Account;
}

async updateAccount(id: string, dto: AccountFormData): Promise<void> {
  if (this.demoMode.isDemo) return;

  const { error } = await this.supabase
    .from('accounts')
    .update({ name: dto.name, type: dto.type, asset_description: dto.asset_description })
    .eq('id', id)
    .eq('user_id', this.auth.userId);
  if (error) throw error;
}

async deleteAccount(id: string): Promise<void> {
  if (this.demoMode.isDemo) return;

  const { error } = await this.supabase
    .from('accounts')
    .delete()
    .eq('id', id)
    .eq('user_id', this.auth.userId);
  if (error) throw error;
}

async setActive(id: string, isActive: boolean): Promise<void> {
  if (this.demoMode.isDemo) return;

  const { error } = await this.supabase
    .from('accounts')
    .update({ is_active: isActive })
    .eq('id', id)
    .eq('user_id', this.auth.userId);
  if (error) throw error;
}
```

Nota su `getLatestBalances()` in demo: `DEMO_SNAPSHOTS` è ordinato da Giu 2025 in poi — il primo snapshot per account_id è il più vecchio. Dobbiamo prendere il più recente. Inverti l'array prima del loop:

```ts
async getLatestBalances(): Promise<Map<string, number>> {
  if (this.demoMode.isDemo) {
    const map = new Map<string, number>();
    for (const snap of [...DEMO_SNAPSHOTS].reverse()) {
      map.set(snap.account_id, snap.value);
    }
    return map;
  }
  // ... resto invariato
}
```

- [ ] **Step 2: Aggiorna accounts.component.ts — nascondi azioni in demo**

Aggiungi import del servizio e inject nella classe:
```ts
import { DemoModeService } from '../../core/demo-mode.service';
```
```ts
demoMode = inject(DemoModeService);
```

Nel template, avvolgi il blocco `.add-row` e i pulsanti di azione con `@if (!demoMode.isDemo)`:

Sostituisci il blocco `<div class="add-row">`:
```html
@if (!demoMode.isDemo) {
  <div class="add-row">
    <p-button icon="pi pi-plus" [label]="'accounts.add' | translate"
              (onClick)="openForm(null)" />
  </div>
}
```

Sostituisci `<div class="account-actions">` nella lista account attivi:
```html
@if (!demoMode.isDemo) {
  <div class="account-actions">
    <p-button icon="pi pi-pencil" [label]="'accounts.edit' | translate"
              severity="secondary" size="small" (onClick)="openForm(account)" />
    <p-button icon="pi pi-inbox" [label]="'accounts.archive' | translate"
              severity="secondary" size="small" (onClick)="openArchiveConfirm(account.id)" />
  </div>
}
```

Sostituisci `<div class="account-actions">` nella lista account archiviati:
```html
@if (!demoMode.isDemo) {
  <div class="account-actions">
    <p-button icon="pi pi-refresh" [label]="'accounts.reactivate' | translate"
              severity="secondary" size="small" (onClick)="reactivate(account.id)" />
    <p-button icon="pi pi-trash" [label]="'accounts.delete' | translate"
              severity="danger" size="small" (onClick)="openDeleteConfirm(account.id)" />
  </div>
}
```

- [ ] **Step 3: Verifica compilazione**

```
npx tsc --noEmit
```
Atteso: nessun errore.

- [ ] **Step 4: Commit**

```bash
git add src/app/features/accounts/accounts.service.ts src/app/features/accounts/accounts.component.ts
git commit -m "feat: accounts service and component support demo mode"
```

---

## Task 5: MonthlyEntryService + MonthlyEntryComponent

**Files:**
- Modifica: `src/app/features/monthly-entry/monthly-entry.service.ts`
- Modifica: `src/app/features/monthly-entry/monthly-entry.component.ts`

**Interfaces:**
- Consuma: `DemoModeService.isDemo`, `DEMO_SNAPSHOTS`, `DEMO_INCOME`
- Produce: form pre-compilato con dati demo, salvataggio disabilitato

- [ ] **Step 1: Aggiorna monthly-entry.service.ts**

Aggiungi import:
```ts
import { DemoModeService } from '../../core/demo-mode.service';
import { DEMO_SNAPSHOTS, DEMO_INCOME } from '../../core/demo-data';
```

Aggiungi inject nella classe:
```ts
private demoMode = inject(DemoModeService);
```

Aggiorna i quattro metodi:
```ts
async getSnapshotsForMonth(year: number, month: number): Promise<MonthlySnapshot[]> {
  if (this.demoMode.isDemo) {
    return DEMO_SNAPSHOTS.filter(s => s.year === year && s.month === month);
  }

  const { data, error } = await this.supabase
    .from('monthly_snapshots')
    .select('*')
    .eq('user_id', this.auth.userId)
    .eq('year', year)
    .eq('month', month);
  if (error) throw error;
  return (data ?? []) as MonthlySnapshot[];
}

async getIncomeForMonth(year: number, month: number): Promise<IncomeTransaction[]> {
  if (this.demoMode.isDemo) {
    return DEMO_INCOME.filter(i => i.year === year && i.month === month);
  }

  const { data, error } = await this.supabase
    .from('income_transactions')
    .select('*')
    .eq('user_id', this.auth.userId)
    .eq('year', year)
    .eq('month', month);
  if (error) throw error;
  return (data ?? []) as IncomeTransaction[];
}

async saveSnapshots(snapshots: SnapshotUpsertData[]): Promise<void> {
  if (this.demoMode.isDemo) return;

  if (snapshots.length === 0) return;
  const rows = snapshots.map(s => ({ ...s, user_id: this.auth.userId }));
  const { error } = await this.supabase
    .from('monthly_snapshots')
    .upsert(rows, { onConflict: 'user_id,account_id,year,month' });
  if (error) throw error;
}

async saveIncome(year: number, month: number, items: IncomeFormData[]): Promise<void> {
  if (this.demoMode.isDemo) return;

  const { error: delError } = await this.supabase
    .from('income_transactions')
    .delete()
    .eq('user_id', this.auth.userId)
    .eq('year', year)
    .eq('month', month);
  if (delError) throw delError;

  if (items.length === 0) return;

  const rows = items.map(i => ({ ...i, year, month, user_id: this.auth.userId }));
  const { error } = await this.supabase
    .from('income_transactions')
    .insert(rows);
  if (error) throw error;
}
```

- [ ] **Step 2: Aggiorna monthly-entry.component.ts — bypass profilo e disabilita salvataggio**

Aggiungi import:
```ts
import { DemoModeService } from '../../core/demo-mode.service';
```

Aggiungi inject nella classe:
```ts
demoMode = inject(DemoModeService);
```

Aggiorna `ngOnInit()` per saltare `profileService.getProfile()` in demo:
```ts
async ngOnInit() {
  if (this.demoMode.isDemo) {
    this.preferredDay = 27;
    await this.load();
    return;
  }
  const profile = await this.profileService.getProfile();
  this.preferredDay = profile.preferred_day;
  await this.load();
}
```

Aggiorna `load()` per usare Jun 2026 come mese di default in demo (l'ultimo mese con dati):
```ts
async ngOnInit() {
  if (this.demoMode.isDemo) {
    this.preferredDay = 27;
    this.selectedDate = new Date(2026, 5, 1); // Giu 2026 — ultimo mese demo
    await this.load();
    return;
  }
  const profile = await this.profileService.getProfile();
  this.preferredDay = profile.preferred_day;
  await this.load();
}
```

Nel template, disabilita il pulsante Salva e il pulsante "Copia dal mese precedente" in demo mode. Sostituisci il pulsante Salva:
```html
<p-button [label]="saving ? ('entry.saving' | translate) : ('entry.save' | translate)"
          [loading]="saving" (onClick)="onSave()"
          [disabled]="demoMode.isDemo"
          [title]="demoMode.isDemo ? 'Non disponibile in modalità demo' : ''"
          styleClass="w-full" />
```

Sostituisci il pulsante "Copia dal mese precedente":
```html
<p-button icon="pi pi-copy" [label]="'entry.copy_prev' | translate"
          severity="secondary" size="small" [loading]="copying"
          [disabled]="demoMode.isDemo"
          (onClick)="copyFromPreviousMonth()" />
```

- [ ] **Step 3: Verifica compilazione**

```
npx tsc --noEmit
```
Atteso: nessun errore.

- [ ] **Step 4: Commit**

```bash
git add src/app/features/monthly-entry/monthly-entry.service.ts src/app/features/monthly-entry/monthly-entry.component.ts
git commit -m "feat: monthly entry service and component support demo mode"
```

---

## Task 6: ShellComponent + NavComponent

**Files:**
- Modifica: `src/app/shared/layout/shell/shell.component.ts`
- Modifica: `src/app/shared/layout/nav/nav.component.ts`

**Interfaces:**
- Consuma: `DemoModeService.isDemo`, `DemoModeService.exit()`
- Produce: skip onboarding in demo; badge DEMO + link "Esci dalla demo" nella nav

- [ ] **Step 1: Aggiorna shell.component.ts**

Aggiungi import e inject:
```ts
import { DemoModeService } from '../../../core/demo-mode.service';
```
```ts
private demoMode = inject(DemoModeService);
```

Aggiorna `ngOnInit()`:
```ts
async ngOnInit() {
  if (this.demoMode.isDemo) return;
  try {
    const profile = await this.profileService.getProfile();
    if (!profile.onboarding_completed) {
      this.router.navigate(['/onboarding']);
    }
  } catch {
    // Profile not yet available — auth state may still be settling
  }
}
```

- [ ] **Step 2: Aggiorna nav.component.ts — badge DEMO + exit**

Aggiungi import e inject:
```ts
import { DemoModeService } from '../../../core/demo-mode.service';
```
```ts
demoMode = inject(DemoModeService);
```

Nel template del brand (sidebar), aggiungi il badge subito dopo `brand-name`:
```html
@if (mode === 'sidebar') {
  <div class="brand">
    <span class="brand-mark">€</span>
    <span class="brand-name">FindMyFunds</span>
    @if (demoMode.isDemo) {
      <span class="demo-badge">DEMO</span>
    }
  </div>
}
```

Aggiungi un link "Esci dalla demo" in fondo agli item della nav, visibile solo in demo:
```html
@if (demoMode.isDemo) {
  <button class="nav-item exit-demo-btn" (click)="demoMode.exit()">
    <i class="pi pi-sign-out nav-icon"></i>
    <span class="nav-label">Esci dalla demo</span>
  </button>
}
```

Aggiungi gli stili:
```css
.demo-badge {
  font-size: 0.6rem;
  font-weight: 700;
  background: #22c55e;
  color: white;
  padding: 0.15rem 0.4rem;
  border-radius: 4px;
  letter-spacing: 0.05em;
}
.exit-demo-btn {
  background: none;
  border: none;
  cursor: pointer;
  width: 100%;
  text-align: left;
  color: #6b7280;
  font-size: 0.95rem;
}
.exit-demo-btn:hover { background: #fee2e2; color: #b91c1c; }
```

- [ ] **Step 3: Verifica compilazione**

```
npx tsc --noEmit
```
Atteso: nessun errore.

- [ ] **Step 4: Commit**

```bash
git add src/app/shared/layout/shell/shell.component.ts src/app/shared/layout/nav/nav.component.ts
git commit -m "feat: shell skips onboarding in demo; nav shows DEMO badge and exit link"
```

---

## Task 7: LoginComponent e SettingsComponent

**Files:**
- Modifica: `src/app/features/auth/login/login.component.ts`
- Modifica: `src/app/features/settings/settings.component.ts`

**Interfaces:**
- Consuma: `DemoModeService.isDemo` (settings), `Router` (login → /demo)
- Produce: bottone "Prova la demo →" nel login; banner demo nelle impostazioni

- [ ] **Step 1: Aggiorna login.component.ts**

Il componente usa `Router` già importato. Aggiungi il bottone sotto il link di registrazione:

Nel template, dopo `<p class="auth-link">...</p>`:
```html
<div class="demo-divider">
  <span>oppure</span>
</div>
<p-button
  label="Prova la demo →"
  severity="secondary"
  styleClass="w-full"
  (onClick)="goToDemo()" />
```

Aggiungi il metodo nella classe:
```ts
goToDemo() {
  this.router.navigate(['/demo']);
}
```

Aggiungi lo stile:
```css
.demo-divider {
  display: flex; align-items: center; gap: 0.75rem;
  margin: 1rem 0 0.75rem;
  color: #9ca3af; font-size: 0.85rem;
}
.demo-divider::before,
.demo-divider::after {
  content: ''; flex: 1; height: 1px; background: #e5e7eb;
}
```

- [ ] **Step 2: Aggiorna settings.component.ts**

Aggiungi import e inject:
```ts
import { DemoModeService } from '../../core/demo-mode.service';
```
```ts
demoMode = inject(DemoModeService);
```

Nel template, avvolgi l'intero contenuto di `<div class="fmf-card settings-form">` con un `@if`:
```html
<h1 class="page-title">{{ 'settings.title' | translate }}</h1>

@if (demoMode.isDemo) {
  <div class="fmf-card demo-notice">
    <i class="pi pi-info-circle" style="font-size:1.5rem; color:#16a34a;"></i>
    <div>
      <p style="margin:0; font-weight:600; color:#166534;">Sei in modalità demo</p>
      <p style="margin:0.25rem 0 0; color:#374151; font-size:0.9rem;">
        Le impostazioni non sono disponibili nella demo.
      </p>
    </div>
    <p-button label="Registrati gratis →" severity="secondary"
              size="small" routerLink="/auth/register" />
  </div>
} @else {
  <div class="fmf-card settings-form">
    <!-- tutto il contenuto esistente invariato -->
  </div>
}
```

Aggiungi `RouterLink` agli imports del componente se non già presente, e lo stile:
```css
.demo-notice {
  display: flex; align-items: center; gap: 1rem;
  flex-wrap: wrap; background: #f0fdf4; border: 1px solid #86efac;
}
```

- [ ] **Step 3: Verifica compilazione completa e tutti i test**

```
npx tsc --noEmit && npx ng test --watch=false --browsers=ChromeHeadless
```
Atteso: compilazione OK, tutti i test PASSED.

- [ ] **Step 4: Commit finale**

```bash
git add src/app/features/auth/login/login.component.ts src/app/features/settings/settings.component.ts
git commit -m "feat: login shows demo entry button; settings shows demo notice"
```

---

## Self-Review vs Spec

| Requisito spec | Task |
|---|---|
| URL `/demo` pubblico | Task 2 |
| Bottone "Prova la demo →" nel login | Task 7 |
| Tutte le sezioni navigabili in sola lettura | Task 3, 4, 5 |
| Badge DEMO nella nav | Task 6 |
| "Esci dalla demo" nella nav | Task 6 |
| Skip onboarding in demo | Task 6 |
| Settings mostra banner demo | Task 7 |
| Entry disabilita salvataggio | Task 5 |
| Accounts nasconde pulsanti azione | Task 4 |
| 5 conti, 12+ mesi di dati, income mensile | Task 1 |
| Persistenza via sessionStorage | Task 1 |
| authGuard bypassa Supabase in demo | Task 2 |
